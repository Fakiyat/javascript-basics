"use strict"; // treats all codes as a newer version

// alert(3 + 3); // we cannot use this in nodejs, it is usable in browser

console.log(3+3);//code should be readable
console.log("hello World!");


// data types Primitive data

let name ="fakiyat";    //string => ""
let age = 20;     // number => 2 to the power 53 if number is bigger then this then we used bigint
let isLoggedIn = true    //boolean => true/false
let state;    //undefined => means the value is not assigned yet
let temp= null;//null => standalone value(its not an empty value its like when i want an output from a server but there is any problem in the server so it will send me null )
//sybmol=> used for uniqueness(like the thing is diffent from other thing in its area )

//Non-premitive data types
//Object

console.log(typeof "name"); //string
console.log(typeof age); //number
console.log(typeof isLoggedIn);//boolean
console.log(typeof undefined);//undefined
console.log(typeof null);//object



